POSTGRES_USER=werewolf
POSTGRES_PASSWORD=werewolf
POSTGRES_DB=werewolf
POSTGRES_PORT=5432

REDIS_PORT=6379

BACKEND_PORT=4000
FRONTEND_PORT=5173

# Uncomment + set when serving frontend through a tunnel/public domain —
# the browser can't reach "localhost" from a remote machine.
# PUBLIC_BACKEND_URL=http://your-public-domain:port

# Admin panel password (Debug + content editor). Change this.
ADMIN_KEY=changeme
